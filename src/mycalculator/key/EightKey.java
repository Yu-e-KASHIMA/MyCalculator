package mycalculator.key;

import mycalculator.config.KeyAllocator;

public class EightKey {

	public EightKey(KeyAllocator allocator) {
		allocator.label = "8";
		allocator.functionId = 1;
		allocator.status = 8;
	}

}
