package mycalculator.key;

import mycalculator.utility.KeyAllocator;

public class MinusKey {

	public MinusKey(KeyAllocator allocator) {
		allocator.label = "－";
		allocator.functionId = 2;
		allocator.status = 2;
	}

}
