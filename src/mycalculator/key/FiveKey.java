package mycalculator.key;

import mycalculator.utility.KeyAllocator;

public class FiveKey {

	public FiveKey(KeyAllocator allocator) {
		allocator.label = "5";
		allocator.functionId = 1;
		allocator.status = 5;
	}

}
