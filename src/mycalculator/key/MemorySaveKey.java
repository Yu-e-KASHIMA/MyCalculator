package mycalculator.key;

import mycalculator.utility.KeyAllocator;

public class MemorySaveKey {

	public MemorySaveKey(KeyAllocator allocator) {
		allocator.label = "MS";
		allocator.functionId = 4;
		allocator.status = 0;
	}

}
